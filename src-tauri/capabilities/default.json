{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for NoteShot windows",
  "windows": ["main", "capture-popup"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
